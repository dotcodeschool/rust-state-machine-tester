#[test]
fn test_block_type_definitions() {
    // These should compile if types are defined correctly
    let _extrinsic: crate::types::Extrinsic;
    let _header: crate::types::Header;
    let _block: crate::types::Block;
    assert!(true);
}